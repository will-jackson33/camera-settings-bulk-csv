# Runtime dependencies of the editor and the compare tool. One: openpyxl reads .xlsx edit sheets
# directly, the standard library cannot, and doing without it means saving every sheet as CSV first.
openpyxl==3.1.5
